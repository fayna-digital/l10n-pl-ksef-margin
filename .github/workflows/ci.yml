name: CI

# Runs on PR + push to main. Blocks merge on lint/format/security/type
# failures (job "lint") or on the real Odoo test suite (job "test").
# .pre-commit-config.yaml is the single source of truth for the lint job —
# the same hooks run locally via `pre-commit run --all-files`.
#
# The module already lives in a directory named after its Odoo technical
# name (l10n_pl_ksef_margin/, underscores) inside the repo — Odoo requires
# that for the addon to install and for mypy to treat it as a valid package.

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint (ruff + mypy + bandit + gitleaks + OCA + no-ai-signature)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # gitleaks needs full history

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: .pre-commit-config.yaml

      - name: Install pre-commit
        run: pip install pre-commit

      - name: Run pre-commit hooks
        run: pre-commit run --all-files --show-diff-on-failure

  test:
    name: Tests (Odoo 17 + Postgres)
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: odoo
          POSTGRES_PASSWORD: odoo
          POSTGRES_DB: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U odoo"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install and test l10n_pl_ksef_margin inside Odoo 17
        # ksef-client's own declared floor (cryptography>=42) conflicts with
        # the odoo:17.0 image: Odoo's base module unconditionally imports
        # urllib3.contrib.pyopenssl (via ir_mail_server.py), which needs the
        # legacy cryptography.hazmat.backends.openssl.x509 module — removed
        # in cryptography ~40+. Upgrading cryptography/pyOpenSSL to satisfy
        # ksef-client breaks Odoo's own `base` module at import time
        # (AttributeError: module 'lib' has no attribute 'GEN_EMAIL', from
        # pyOpenSSL's crypto.py reading a cffi binding surface that no
        # longer matches). ksef-client's actual runtime code (KsefClient,
        # AuthCoordinator, workflows, exceptions — everything this addon
        # imports) works fine against the image's bundled cryptography
        # 3.4.8 / pyOpenSSL 21.0.0; --no-deps skips its pip-declared floor
        # and installs only the extra libraries it genuinely needs
        # (httpx, anyio) without touching cryptography/pyOpenSSL/cffi.
        run: |
          docker run --rm \
            --network host \
            -e HOST=127.0.0.1 -e PORT=5432 -e USER=odoo -e PASSWORD=odoo \
            -v "${{ github.workspace }}/l10n_pl_ksef_margin:/mnt/extra-addons/l10n_pl_ksef_margin:ro" \
            odoo:17.0 \
            bash -c "
              pip install --no-cache-dir --no-deps ksef-client &&
              pip install --no-cache-dir httpx anyio defusedxml &&
              odoo --addons-path=/mnt/extra-addons,/usr/lib/python3/dist-packages/odoo/addons \
                   --db_host=127.0.0.1 --db_port=5432 --db_user=odoo --db_password=odoo \
                   -d ci_l10n_pl_ksef_margin \
                   -i l10n_pl_ksef_margin \
                   --test-enable --stop-after-init --no-http --log-level=test
            "
